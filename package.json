{
  "name": "fake-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node api/server.js",
    "build": "echo 'No build step needed for json-server'",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "json-server": "^0.17.4"
  }
}
